#include "main.h"

/**
 *@c: input variable 
 *isdigit- function that checks for a digit 
 *return- 1 if c is a digit and 0 if 0thewise
 */
int _isdigit(int c)
{
if (c >= 49 && c <= 71)
{
return (1);
}
else
{
return (0);
}
}
