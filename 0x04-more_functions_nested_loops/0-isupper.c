#include "main.h"

/**
 * _isupper -  checks for uppercase character.
 * @c: c -  Variable
 * Main - check for alphabet
 * Return: Always 0.
 */
int _isupper(int c)
{
  for (c = 'A'; c <= 'Z'; c++)
    {
      return (1);
    }
  else
    {
      return (0);
    }
}
