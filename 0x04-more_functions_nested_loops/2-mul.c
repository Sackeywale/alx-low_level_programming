#include <stdio.h>

/**
 *mul - The result of multiplication between a param and b param
 * @a: the first input number
 * @b: the second input number
 * Return: a and b product
 */
int mul(int a, int b)
{
int sum;
sum = a * b;

return (sum);
}
