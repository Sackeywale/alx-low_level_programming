#include "main.h"

/**
 *_islower - check lower case integer
 *return 1 if lower case and 0 if otherwise
 */

int _islower(int c)
{
int c;

if (c = 'a' && c <= 'z')
{
return (0);
}
else
return (1)   
}
